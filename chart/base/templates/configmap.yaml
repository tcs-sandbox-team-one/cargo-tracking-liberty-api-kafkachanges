apiVersion: v1
data:
  trackingdburl: "{{ .Values.datasources.dburl }}"
  rabbitmqhost: "{{ .Values.datasources.mqhost }}"
  rabbitmqport: "{{ .Values.datasources.mqport }}" 
kind: ConfigMap
metadata:
  name: tracking-config